<header class="bg-gray-800 p-6 mb-8">
  <div class="container mx-auto flex justify-between items-center">
    <a
      href="/dashboard"
      class="text-white font-bold text-lg focus:outline-none focus:ring-0 focus:ring-offset-0"
    >
      <h1 class="text-3xl font-bold text-white">QR-MATE</h1>
    </a>
   
    <a
      href="/sign-out"
      class="bg-red-500 text-white py-2 px-4 rounded-md hover:bg-red-600 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2"
      title="Sign Out"
    >
    <i class="fa-solid fa-right-from-bracket"></i>
    </a>


  </div>
</header>

<!-- Main Content -->
<div class="container mx-auto p-4 mt-6">
  <div class="max-w-4xl mx-auto  p-6 rounded-lg shadow-lg">
    <%- include('../partials/error') %> <%- include('../partials/success') %>
    <form id="settingsForm">
      <!-- Title  -->

      <div class="mb-6">
        <label for="expiryTime" class="block text-sm font-medium text-gray-400">
          Voucher Title
        </label>
        <input
          type="text"
          id="title"
          value="<%= settings.title %>"
          name="title"
          class="mt-2 p-3 border border-gray-300 rounded-md w-full bg-gray-300"
        />
        <p id="titleError" class="text-red-500 text-sm mt-2"></p>
      </div>
      <!-- Expiry Time -->
      <div class="mb-6">
        <label for="expiryTime" class="block text-sm font-medium  text-gray-400">
          Maximum Allowed Expiry Time (in days)
        </label>
        <input
          type="number"
          value="<%= settings.expiry_days %>"
          id="expiryTime"
          name="expiryTime"
          class="mt-2 p-3 border border-gray-300 rounded-md w-full bg-gray-300"
        />
        <p id="expiryTimeError" class="text-red-500 text-sm mt-2"></p>
      </div>

      <!-- Voucher Width -->
      <div class="mb-6">
        <label for="width" class="block text-sm font-medium  text-gray-400"
          >Voucher Width (mm)</label
        >
        <input
          type="number"
          id="width"
          value="<%= settings.width_mm %>"
          name="width"
          class="mt-2 p-3 border border-gray-300 rounded-md w-full bg-gray-300"
        />
        <p id="widthError" class="text-red-500 text-sm mt-2"></p>
      </div>

      <!-- Voucher Height -->
      <div class="mb-6">
        <label for="height" class="block text-sm font-medium  text-gray-400"
          >Voucher Height (mm)</label
        >
        <input
          type="number"
          id="height"
          value="<%= settings.height_mm %>"
          name="height"
          class="mt-2 p-3 border border-gray-300 rounded-md w-full bg-gray-300"
        />
        <p id="heightError" class="text-red-500 text-sm mt-2"></p>
      </div>

      <!-- Title Font Size -->
      <div class="mb-6">
        <label
          for="titleFontSize"
          class="block text-sm font-medium  text-gray-400"
          >Title Font Size (px)</label
        >
        <input
          type="number"
          id="titleFontSize"
          value="<%= settings.title_font %>"
          name="titleFontSize"
          class="mt-2 p-3 border border-gray-300 rounded-md w-full bg-gray-300"
        />
        <p id="titleFontSizeError" class="text-red-500 text-sm mt-2"></p>
      </div>

      <!-- Text Font Size -->
      <div class="mb-6">
        <label
          for="textFontSize"
          class="block text-sm font-medium  text-gray-400"
          >Normal Text Font Size (px)</label
        >
        <input
          type="number"
          id="textFontSize"
          value="<%= settings.text_font %>"
          name="textFontSize"
          class="mt-2 p-3 border border-gray-300 rounded-md w-full bg-gray-300" 
        />
        <p id="textFontSizeError" class="text-red-500 text-sm mt-2"></p>
      </div>

      <!-- Submit Button -->
      <div class="text-center">
        <button
          type="submit"
          class="border text-white px-6 py-3 rounded-md hover:bg-blue-700 focus:outline-none"
        >
          Save
        </button>
      </div>
    </form>
  </div>
</div>

<script type="module" src="/javascripts/validation/voucher.js"></script>
